// An array is a collection of elements (which can be of different or the same data types) stored in a single variable.
// In JavaScript, arrays are a special type of object that allows you to store and manage ordered lists of values.

let fruits = ["apple", "banana", "cherry"];

console.log(fruits[0]); 
console.log(fruits[1]); 
console.log(fruits.length); 
